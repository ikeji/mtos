// hello2.tc — second guest task: prints "WORLD"

fn main() -> i32 {
    var buf: U8Array = U8Array(6);
    set(buf, 0, 'W'); set(buf, 1, 'O'); set(buf, 2, 'R');
    set(buf, 3, 'L'); set(buf, 4, 'D'); set(buf, 5, 10u8);
    sys_write(1, buf, 6);
    delete(buf);
    return 0;
}
