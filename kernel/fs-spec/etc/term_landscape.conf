# Terminal geometry for /bin/console -l (landscape LCD).
# Read by full-screen apps (sl, vi, ...) to size the visible window.
# Lines are KEY=VALUE; lines starting with # and blank lines are ignored.
COLS=56
ROWS=15
