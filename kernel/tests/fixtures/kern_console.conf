# kern.conf — seeds /bin/console alone (フェーズ9 S7).
#
# At boot the kernel launches /bin/console as the init task. console
# opens /dev/kbd + /dev/fb, spawns a nested /bin/sh over a pipe, and
# renders that shell's output to /dev/fb — i.e. the LCD console comes
# up directly, no separate UART shell.
#
# Only console is seeded: on qemu virt /dev/kbd is the UART-RX stub,
# so a second `init=/bin/sh` would fight console's sh for UART input.
# On real hardware (GPIO-matrix /dev/kbd) the two-shell layout
# `init=/bin/sh` + `init=/bin/console` is contention-free.
init=/bin/console
